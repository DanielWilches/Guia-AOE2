import { CapatilizacionPipe } from './capatilizacion.pipe';

describe('CapatilizacionPipe', () => {
  it('create an instance', () => {
    const pipe = new CapatilizacionPipe();
    expect(pipe).toBeTruthy();
  });
});
