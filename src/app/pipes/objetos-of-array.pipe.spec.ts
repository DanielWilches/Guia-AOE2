import { ObjetosOfArrayPipe } from './objetos-of-array.pipe';

describe('ObjetosOfArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjetosOfArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
